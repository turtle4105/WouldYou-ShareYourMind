<UserControl x:Class="WouldYou_ShareMind.Views.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="400" d:DesignHeight="750">

    <Grid Margin="20,24,20,12">

        <!-- ✅ 1) 리소스는 가장 먼저 -->
        <Grid.Resources>
            <Style x:Key="SettingRow" TargetType="Button" BasedOn="{StaticResource Btn.Secondary}">
                <Setter Property="Height" Value="58"/>
                <Setter Property="Padding" Value="14,10"/>
                <Setter Property="Margin" Value="0,10,0,0"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Background" Value="{StaticResource Brush.PanelAlt}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Grad.Border}"/>
                <Setter Property="BorderThickness" Value="1.4"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="{StaticResource Cr.Pill}"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter VerticalAlignment="Center" Margin="4,0"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#1AFFFFFF"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.6"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>

        <!-- ✅ 2) 구조 정의 -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- 서브 -->
        <TextBlock Grid.Row="2"
                   Style="{StaticResource Txt.Subtitle}"
                   TextAlignment="Center"
                   Margin="0,15,0,15"
                   Text="편안한 밤을 위한 나만의 설정을 골라보세요." />

        <!-- 항목들 -->
        <StackPanel Grid.Row="4">

            <!-- 언어 -->
            <Button Style="{StaticResource SettingRow}" Command="{Binding OpenLanguageCommand}">
                <Grid Margin="2,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Source="/Resources/images/language.png" Width="26" Height="26" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Style="{StaticResource Txt.Body}" FontSize="15" VerticalAlignment="Center" Text="언어"/>
                </Grid>
            </Button>

            <!-- 테마 -->
            <Button Style="{StaticResource SettingRow}" Command="{Binding OpenThemeCommand}">
                <Grid Margin="2,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Source="/Resources/images/theme.png" Width="26" Height="26" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Style="{StaticResource Txt.Body}" FontSize="15" VerticalAlignment="Center" Text="테마"/>
                </Grid>
            </Button>

            <!-- 사운드 -->
            <Button Style="{StaticResource SettingRow}" Command="{Binding OpenSoundCommand}">
                <Grid Margin="2,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Source="/Resources/images/soundsetting.png" Width="26" Height="26" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Style="{StaticResource Txt.Body}" FontSize="15" VerticalAlignment="Center" Text="사운드"/>
                </Grid>
            </Button>

            <!-- 호흡 가이드 -->
            <Button Style="{StaticResource SettingRow}" Command="{Binding OpenBreatheGuideCommand}">
                <Grid Margin="2,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Source="/Resources/images/breathe.png" Width="26" Height="26" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Style="{StaticResource Txt.Body}" FontSize="15" VerticalAlignment="Center" Text="호흡 가이드"/>
                </Grid>
            </Button>

            <!-- 타이머 -->
            <Button Style="{StaticResource SettingRow}" Command="{Binding OpenTimerCommand}">
                <Grid Margin="2,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Source="/Resources/images/timer.png" Width="26" Height="26" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="2" Style="{StaticResource Txt.Body}" FontSize="15" VerticalAlignment="Center" Text="타이머"/>
                </Grid>
            </Button>

        </StackPanel>
    </Grid>
</UserControl>
